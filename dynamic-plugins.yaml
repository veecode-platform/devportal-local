# DERIVED FILE — do not hand-edit.
#
# This is the chart's own rendered `dynamic-plugins.yaml` (the ConfigMap the
# Helm chart builds from global.dynamic) at the tag in .chart-pin, so the
# compose harness installs exactly what the chart installs. Re-render it
# whenever .chart-pin moves.
#
# Since chart-v0.1.10 (OD1 phase A), the VeeCode product face (Home, header,
# TechDocs, Notifications, Tech Radar, Marketplace, theme — 20 plugins) is no
# longer inline here under `plugins:`. It moved into an image-baked includes
# file, dynamic-plugins.veecode.yaml, referenced below ahead of the
# marketplace write-through file — global.dynamic.plugins is now purely
# additive customer input, and stays empty by default. See
# devportal-chart/docs/product-face-overrides.md.
includes:
- /opt/app-root/src/dynamic-plugins.veecode.yaml
- /devportal-data/extensions-install.yaml
plugins: []
